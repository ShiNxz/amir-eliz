/**
 * Since the Navbar isn't fixed, we will show the user a SideNav when they scroll down the page.
 */
const SideNav = () => {
	return <div className='fixed right-0 p-4'></div>
}

export default SideNav
